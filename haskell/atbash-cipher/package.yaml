name: atbash-cipher
version: 1.2.0.5

dependencies:
  - base

library:
  exposed-modules: Atbash
  source-dirs: src
  dependencies:
    - containers
    - split

tests:
  test:
    main: Tests.hs
    source-dirs: test
    dependencies:
      - atbash-cipher
      - hspec
